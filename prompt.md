##  Validation

Explain what a Regular Expression (Regex) is. I need to validate
an email address input in my React form. What does the pattern
/^[^\s@]+@[^\s@]+\.[^\s@]+$/ mean? Break it down character by character.


In my React registration form, I need to validate:
1. Required fields (not empty)
2. Valid email format
3. Password minimum 8 characters
4. Password and confirm password must match

## Debugging


My React component is re-rendering on every keystroke when the user
types in an input field. Is this normal behavior? When does React
re-render a component and is there anything I should be concerned about
for a simple form like this?

I am getting this error in my React app:
"Cannot read properties of undefined (reading 'firstName')"
This happens when I try to access formData.firstName in my child component.
What are the common causes of this error and how do I debug it?


My useState update is not reflecting immediately after I call the setter.
I do setFormData and then console.log(formData) but it still shows the
old value. Why does this happen in React and how should I handle it?



##  Submission & Data


On the final step of my registration wizard, I need to console.log
the complete form data object when the user clicks Submit. I also need
to show a success UI after submission. How should I structure this
in React — should the success screen be a new component or just a
conditional render inside the same component?


Explain the difference between e.preventDefault() and type="button"
on a button element inside a form. When should I use each approach
to prevent page refresh on form submission?
